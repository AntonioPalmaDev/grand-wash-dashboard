-- Ensure 'visualizador' and 'anonimo' roles are handled in RLS
-- This assumes profiles are already set up.

-- If we need to allow specific access for an "anonymous" user profile
-- We should ensure they can see dashboards.

-- Create a policy if needed, but usually it's handled by checking the role in the app.
